{
  "name": "goshats",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:customer": "turbo dev --filter=@goshats/customer",
    "dev:rider": "turbo dev --filter=@goshats/rider",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
